server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/songintae/spring-cloud-config-repository